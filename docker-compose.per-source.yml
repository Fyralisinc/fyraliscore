# GENERATED by scripts/gen_per_source_compose.py — DO NOT EDIT BY HAND.
# Per-source-isolated ingestion worker topology (source-isolation).
# Re-generate after changing RawEnvelope.SourceLiteral:
#   python scripts/gen_per_source_compose.py > docker-compose.per-source.yml
#
# Use WITH the base file, all-sources singletons scaled to 0:
#   docker compose -f docker-compose.yml -f docker-compose.per-source.yml \
#       up -d --scale normalizer=0 --scale observation_writer=0 \
#       --scale embedding_worker=0


services:
  # ---- normalizer (per source) ----
  normalizer_slack:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: slack
    env_file: .env.production
    container_name: company_os_normalizer_slack
    command: python -m services.ingestion.normalizer.worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  normalizer_github:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: github
    env_file: .env.production
    container_name: company_os_normalizer_github
    command: python -m services.ingestion.normalizer.worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  normalizer_discord:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: discord
    env_file: .env.production
    container_name: company_os_normalizer_discord
    command: python -m services.ingestion.normalizer.worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  normalizer_gmail:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: gmail
    env_file: .env.production
    container_name: company_os_normalizer_gmail
    command: python -m services.ingestion.normalizer.worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  normalizer_notion:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: notion
    env_file: .env.production
    container_name: company_os_normalizer_notion
    command: python -m services.ingestion.normalizer.worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  normalizer_google_calendar:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: google_calendar
    env_file: .env.production
    container_name: company_os_normalizer_google_calendar
    command: python -m services.ingestion.normalizer.worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  normalizer_google_drive:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: google_drive
    env_file: .env.production
    container_name: company_os_normalizer_google_drive
    command: python -m services.ingestion.normalizer.worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  normalizer_jira:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: jira
    env_file: .env.production
    container_name: company_os_normalizer_jira
    command: python -m services.ingestion.normalizer.worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  # ---- observation-writer (per source) ----
  observation_writer_slack:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: slack
    env_file: .env.production
    container_name: company_os_observation_writer_slack
    command: python -m services.ingestion.writers.observation_writer
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  observation_writer_github:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: github
    env_file: .env.production
    container_name: company_os_observation_writer_github
    command: python -m services.ingestion.writers.observation_writer
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  observation_writer_discord:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: discord
    env_file: .env.production
    container_name: company_os_observation_writer_discord
    command: python -m services.ingestion.writers.observation_writer
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  observation_writer_gmail:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: gmail
    env_file: .env.production
    container_name: company_os_observation_writer_gmail
    command: python -m services.ingestion.writers.observation_writer
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  observation_writer_notion:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: notion
    env_file: .env.production
    container_name: company_os_observation_writer_notion
    command: python -m services.ingestion.writers.observation_writer
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  observation_writer_google_calendar:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: google_calendar
    env_file: .env.production
    container_name: company_os_observation_writer_google_calendar
    command: python -m services.ingestion.writers.observation_writer
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  observation_writer_google_drive:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: google_drive
    env_file: .env.production
    container_name: company_os_observation_writer_google_drive
    command: python -m services.ingestion.writers.observation_writer
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  observation_writer_jira:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: jira
    env_file: .env.production
    container_name: company_os_observation_writer_jira
    command: python -m services.ingestion.writers.observation_writer
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  # ---- embedding-worker (per source) ----
  embedding_worker_slack:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: slack
    env_file: .env.production
    container_name: company_os_embedding_worker_slack
    command: python -m services.ingestion.writers.embedding_worker.embedding_worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  embedding_worker_github:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: github
    env_file: .env.production
    container_name: company_os_embedding_worker_github
    command: python -m services.ingestion.writers.embedding_worker.embedding_worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  embedding_worker_discord:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: discord
    env_file: .env.production
    container_name: company_os_embedding_worker_discord
    command: python -m services.ingestion.writers.embedding_worker.embedding_worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  embedding_worker_gmail:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: gmail
    env_file: .env.production
    container_name: company_os_embedding_worker_gmail
    command: python -m services.ingestion.writers.embedding_worker.embedding_worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  embedding_worker_notion:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: notion
    env_file: .env.production
    container_name: company_os_embedding_worker_notion
    command: python -m services.ingestion.writers.embedding_worker.embedding_worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  embedding_worker_google_calendar:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: google_calendar
    env_file: .env.production
    container_name: company_os_embedding_worker_google_calendar
    command: python -m services.ingestion.writers.embedding_worker.embedding_worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  embedding_worker_google_drive:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: google_drive
    env_file: .env.production
    container_name: company_os_embedding_worker_google_drive
    command: python -m services.ingestion.writers.embedding_worker.embedding_worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

  embedding_worker_jira:
    build: .
    restart: unless-stopped
    depends_on:
      postgres: {condition: service_healthy}
      kafka: {condition: service_healthy}
      minio: {condition: service_healthy}
      redis: {condition: service_healthy}
      migrate: {condition: service_completed_successfully}
      kafka-init: {condition: service_completed_successfully}
      minio-init: {condition: service_completed_successfully}
    environment:
      DATABASE_URL: postgresql://company_os:company_os@postgres:5432/company_os
      OLLAMA_URL: http://ollama:11434
      COMPANY_OS_ENV: prod
      FYRALIS_ENV: prod
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      S3_ENDPOINT_URL: http://minio:9000
      S3_RAW_BUCKET: fyralis-raw
      S3_REGION_NAME: us-east-1
      AWS_ACCESS_KEY_ID: fyralis
      AWS_SECRET_ACCESS_KEY: fyralis-secret-change-me
      REDIS_URL: redis://redis:6379/0
      INGESTION_HEALTH_PORT: "9300"
      # Source isolation: pin this worker to one source's lane.
      INGESTION_SOURCE: jira
    env_file: .env.production
    container_name: company_os_embedding_worker_jira
    command: python -m services.ingestion.writers.embedding_worker.embedding_worker
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://localhost:9300/healthz').status==200 else 1)\""]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 30s

