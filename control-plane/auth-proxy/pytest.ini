[pytest]
# Run from auth-proxy/ so `tests` is importable as a package and the proxy
# modules (config/proxy/tenant_resolver) resolve from the rootdir on sys.path.
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -q
