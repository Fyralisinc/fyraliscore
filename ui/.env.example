# Browser-to-gateway endpoint for the local onboarding console.
NEXT_PUBLIC_FYRALIS_PROVIDER_INGRESS_URL=http://localhost:8000

# Local development only. Mint a short-lived gateway session and set it here;
# do not expose a production bearer token to a browser environment variable.
NEXT_PUBLIC_FYRALIS_GATEWAY_TOKEN=
