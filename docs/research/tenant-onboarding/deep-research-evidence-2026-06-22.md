# Tenant onboarding — deep-research evidence (verified claims)

> Generated 2026-06-22 for [tenant-onboarding-ui spec](../../plans/tenant-onboarding-ui.md).
> 24 claims that survived 3-vote adversarial verification (2/3 refutes required to kill),
> each with its primary source, verbatim quote, and verifier evidence. `[R#]` indices
> match the spec's References table. This tree is `exclude_docs`'d from the MkDocs build.

## Confirmed claims
### [0] Auth0 recommends its Organizations feature as the foundational primitive for B2B multi-tenant login, claiming it simplifies and makes the login system more maintainable and extensible.
Vote: 3-0 · Source: https://auth0.com/docs/get-started/architecture-scenarios/business-to-business/provisioning (primary)
Quote: "If you can utilize the Organizations Feature, it will greatly simplify your login system, making it more maintainable and extendable for the future."
Verifier evidence (high): VERIFIED on the cited primary source. WebFetch of https://auth0.com/docs/get-started/architecture-scenarios/business-to-business/provisioning confirms the verbatim quote appears in a "Best Practice" info box within the "Provisioning users isolated to the organization" section: "If you can utilize the Organizations Feature, it will greatly simplify your login system, making it more maintainable and extendable for the future." This maps directly onto the claim's wording ("simplifies and makes the login system more maintainable and extensible") and is explicitly framed as a best-practice recommendation. The source is first-party (Auth0's own official docs) for a claim about Auth0's own recommendation — ideal source quality. The feature is current (Organizations launched ~2021, not deprecated; page is live). Attempts to refute failed: (a) the quote is conditional ("If you can utilize"), but "recommends" is a fair paraphrase of a Best Practice callout, not a misread; (b) third-party critiques (Scalekit, WorkOS, Descope blogs) attack the feature's *native-ness/quality* but do NOT dispute that Auth0 itself recommends Organizations — they confirm it is "Auth0's recommended B2B model"; (c) "foundational primitive" slightly exceeds the literal quote but is consistent with Auth0's product positioning (auth0.com/organizations, launch blog). This is a low-stakes claim about what a vendor says about its own product, fully backed by the primary source — not an extraordinary claim needing independent corroboration.

### [1] For B2B onboarding, the recommended user-invitation mechanism is the Organizations feature rather than ad-hoc account creation.
Vote: 3-0 · Source: https://auth0.com/docs/get-started/architecture-scenarios/business-to-business/provisioning (primary)
Quote: "The recommended approach for user invite is to use the Organizations Feature to accomplish this."
Verifier evidence (high): The Auth0 B2B Provisioning doc verbatim states: "The recommended approach for user invite is to use the Organizations Feature to accomplish this." Verified via two independent WebFetches of the primary URL (https://auth0.com/docs/get-started/architecture-scenarios/business-to-business/provisioning) plus its mirror (.../architecture-scenarios/implementation/b2b/b2b-provisioning), both returning the identical recommendation. The "rather than ad-hoc account creation" framing is faithful to the page, which explicitly favors admin-led provisioning over having "users sign up and then have an administrator approve them," and notes Organizations "will greatly simplify your login system." Corroborated by independent sources (WorkOS "complete guide to user management for B2B SaaS," Microsoft Entra B2B best practices, Stitchflow 2025 guide) all treating org-scoped admin invitation as the standard B2B pattern — no contradicting source found. Source is Auth0's own first-party product docs, the authoritative primary source for an Auth0-feature recommendation, matching the moderate strength of a "recommended approach" claim. Minor nuance: it is an Auth0-product-specific recommendation, but the research context (a spec naming Auth0/WorkOS) makes the generalization non-misleading. Not outdated; not a marketing/benchmark claim.

### [2] With enterprise SSO connections, user-profile creation is provisioned automatically on the user's first enterprise login (just-in-time provisioning), rather than requiring a pre-created account.
Vote: 3-0 · Source: https://auth0.com/docs/get-started/architecture-scenarios/business-to-business/provisioning (primary)
Quote: "user profile creation happens automatically upon first enterprise login."
Verifier evidence (high): The claim is well-supported and accurately paraphrases its source. The Auth0 B2B provisioning doc states verbatim: "Enterprise sign up is synonymous with sign in via enterprise login—there's no distinction here per se, as user profile creation happens automatically upon first enterprise login" — a near-exact match to the supporting quote and to the claim. This is a definitional, textbook claim about JIT provisioning, not an extraordinary one, and it is independently corroborated by multiple authoritative primary sources that all give the same definition: Oracle Identity Cloud ("automates user account creation when the user first tries to perform SSO and the user doesn't yet exist"), Salesforce ("Just-in-Time user provisioning... create...users the first time they try to log in"), Okta, Atlassian, Microsoft Entra, JumpCloud, and Clerk. JumpCloud explicitly notes JIT "eliminates the need to manually or bulk create the user accounts upfront." Source quality is high (Auth0/Okta is a primary identity-vendor) and matches the claim's strength. The semantics are stable, not fast-moving (a 2026-dated OneUptime source restates the same definition), so it is not outdated. It is technical documentation, not marketing fluff or a cherry-picked benchmark. The only minor caveat — JIT must be enabled/configured on the SSO connection — does not contradict the claim, which is scoped to "with enterprise SSO connections [using] just-in-time provisioning." No credible source disputes or heavily qualifies the core assertion. Found 0 of 2 needed refutations.

### [3] Ory Polis (formerly BoxyHQ SAML Jackson) is a self-hostable enterprise SSO service that implements SAML 2.0 and OIDC single sign-on by abstracting it as a standard OAuth 2.0 flow, so app developers need only OAuth/OIDC knowledge rather than SAML expertise.
Vote: 3-0 · Source: https://github.com/ory/polis (primary)
Quote: "implements SSO as an OAuth 2.0 flow, abstracting away all the complexities of the SAML protocol"
Verifier evidence (high): Every component of the claim is confirmed by the primary source (official Ory GitHub repo + ory.com docs). README: "Ory Polis - formerly known as BoxyHQ Jackson - bridges or proxies a SAML login flow to OAuth 2.0 or OpenID Connect, abstracting away all the complexities of the SAML protocol." Docs: "Presents the SSO process as a standard OAuth 2.0 flow. Ideal for developers already familiar with OAuth 2.0 and OpenID Connect" — directly supporting the "developers need only OAuth/OIDC knowledge rather than SAML expertise" portion. Self-hostable confirmed: "As an open-source solution, Ory Polis allows you to host the service yourself." SAML 2.0 + OIDC + SCIM 2.0 support confirmed in repo description. The "formerly BoxyHQ SAML Jackson" rename is accurate (built on @boxyhq/saml-jackson core lib). The supporting quote matches the README verbatim. No contradicting or qualifying source found across DeepWiki, Ory blog/docs, and mirrors — all corroborate. This is a factual/architectural product description, not a marketing benchmark or comparative claim, so source quality (vendor's own primary docs) matches the claim's strength.

### [4] Ory Polis supports SCIM 2.0 directory sync for automatic user and group provisioning and de-provisioning, directly covering the SCIM provisioning requirement for enterprise tenant onboarding.
Vote: 3-0 · Source: https://github.com/ory/polis (primary)
Quote: "Directory Sync via the SCIM 2.0 protocol for automatic user and group provisioning/de-provisioning"
Verifier evidence (high): Claim is verified and faithful to the primary source. The official ory/polis GitHub repository description states verbatim: "it also supports Directory Sync via the SCIM 2.0 protocol for automatic user and group provisioning/de-provisioning" — matching the supporting quote exactly with no overreach. Independent confirmation from Ory's official product page (ory.com/directory-sync): "Ory solutions fully support the SCIM 2.0 protocol" and "Automatically sync users, groups, and roles via SCIM. Enable real-time provisioning and deprovisioning to keep identity data current." Ory Polis is the rebrand of BoxyHQ SAML Jackson under Ory, supporting SCIM providers including Okta, OneLogin, Azure/Entra ID, and JumpCloud (per-provider docs found). All searched sources corroborate; none dispute or qualify the claim. Source quality is primary/authoritative (official project repo + official product docs), matching the claim's strength. The claim is a verifiable feature-existence statement, not a benchmark or marketing performance claim, and is current as of 2026. Note: BoxyHQ is already named as a reference pattern in the research question's competitor list, so Ory Polis (its successor) is directly on-topic for the SCIM provisioning angle.

### [5] OAuth/API authentication is deceptively deep: Nango implemented auth for over 800 APIs, a task they estimated at a few weeks but which took three years, because every API has non-standard extensions, parameters, and custom scope separators.
Vote: 3-0 · Source: https://nango.dev/blog/api-auth-is-deep (primary)
Quote: "We implemented auth for over 800 APIs. We thought it would take a few weeks. It took three years."
Verifier evidence (high): The claim is a faithful, verbatim-backed paraphrase of the primary source. Fetching https://nango.dev/blog/api-auth-is-deep confirmed the exact quote: "We implemented auth for over 800 APIs. We thought it would take a few weeks. It took three years." The reasoning is also directly quoted: "every API has non-standard extensions, parameters, custom scope separators, and other quirks." The "800+ APIs" figure is corroborated across independent pages (Y Combinator, Grokipedia, Gradient Ventures). The "three years" timeframe is plausible and consistent with company history: Nango was founded in 2022 (first commit Sept 2022), launched supporting ~40 APIs, and grew to 800+ by 2026. A companion Nango post (why-is-oauth-still-hard) substantiates the per-API quirks with concrete, independently-verifiable examples (QuickBooks realmID, Slack user_scopes, Notion dropping the scope parameter, LinkedIn breaking on PKCE), showing the underlying technical point is real, not invented. CAVEATS: (1) Source is a vendor marketing blog and inherently self-interested; (2) the "few weeks vs three years" is a rhetorical narrative framing of their own product-build experience, not a formally tracked engineering estimate. But for a claim of the form "Nango reports their own auth implementation took ~3 years," the primary first-hand source is appropriately authoritative, the claim does not overreach the quote, it is current (2026), and no contradicting evidence exists. Not refuted.

### [6] Token refresh is a major source of connector failures due to race conditions (some APIs revoke the current access token as soon as a refresh starts) and refresh-token rotation drift (some APIs return a new refresh token on each refresh, others do not).
Vote: 3-0 · Source: https://nango.dev/blog/api-auth-is-deep (primary)
Quote: "Race conditions. Many APIs revoke the current access token as soon as a refresh starts."
Verifier evidence (high): Both halves of the claim are directly supported by the primary source and independently corroborated. (1) Quote match: WebFetch of nango.dev/blog/api-auth-is-deep confirms verbatim "Many APIs revoke the current access token as soon as a refresh starts. If any requests go out during this time, they will fail" (race-condition half) AND "Some APIs return a new refresh token during each refresh, while others do not. You need to store the latest version, or you will lose access" (rotation-drift half) — so the claim is NOT an overreach. (2) Independent corroboration, not just self-referential: RFC 6749 mandates no specific refresh-token expiration/rotation behavior (providers vary — fixed/rolling/single-use), and RFC 9700/OAuth 2.1 require rotation (new refresh token each use, client must store the new one). Auth0 docs, Obsidian Security, and ServiceNow community docs all confirm rotation drift and refresh concurrency/race conditions as real, well-known failure modes. A separate Nango article (concurrency-with-oauth-token-refreshes) describes the Process A/Process B simultaneous-refresh race and notes "Many APIs issue a new refresh token with each refresh... a race condition could lead to the loss of your valid refresh token." (3) Named real-world offenders are documented: LinkedIn and Microsoft Graph produce production invalid_grant failures (Microsoft Q&A threads, LinkedIn token docs). (4) Source quality matches claim strength — this is a moderate engineering claim (not extraordinary), and Nango is a credible OAuth-integration domain authority backed here by IETF RFCs. (5) Current: RFC 9700 / OAuth 2.1 are recent and reinforce the claim; not outdated. Minor caveat only: "revoke access token as soon as refresh starts" is less universally documented across providers than rotation drift, and "major source" is a qualitative judgment — but the underlying factual assertions both hold. Claim survives refutation attempt.

### [7] Nango provides an embeddable, pre-built Connect UI that handles the entire OAuth authorization flow on behalf of a product — including authorization, credential storage, refresh, and validation — with API-specific guidance, input validation, and the product's own branding.
Vote: 3-0 · Source: https://docs.nango.dev/guides/auth/auth-guide (primary)
Quote: "Embed a Nango-managed auth flow in your application, and Nango handles authorization, credential storage, refresh, and validation."
Verifier evidence (high): The claim is well-supported by its own primary source. The cited page (https://nango.dev/docs/guides/auth/auth-guide, redirect of docs.nango.dev/guides/auth/auth-guide) was fetched directly and confirms verbatim: "Pre-built UI — Embedded Connect UI with API-specific guidance, input validation, and your branding," and that the Connect UI lets users "connect 800+ external APIs" while "Nango handles authorization, credential storage, refresh, and validation." Every element of the claim maps to documented text: (a) embeddable/pre-built = "Embedded Connect UI"/"Pre-built UI"; (b) handles authorization + credential storage + refresh + validation = the supporting quote verbatim; (c) API-specific guidance + input validation + own branding = the "Pre-built UI" bullet verbatim. The supporting quote is accurate and not a misread. A confirmatory WebSearch corroborated the same features (input validation "available only for integrations using Nango's Connect UI"; white-label/custom branding). A skeptical search for contradictions found only minor, non-refuting issues (a self-hosting apiUrl config gotcha in GitHub issue #5432; requests for more hosted-UI customization; general "OAuth is still hard" commentary) — none dispute the capability claimed. Source quality is appropriate: this is a descriptive product-capability claim, and the official vendor docs are the authoritative source for a product's own feature set. Docs are current (2026). No marketing-fluff, outdated, or unsupported concern applies.

### [8] The connector auth flow follows a four-step server-driven pattern: the backend requests a session token, the frontend opens the Connect UI with that token, the user authorizes the external API, and the backend receives a webhook with the connection ID — a concrete template for embedding connector auth into an onboarding wizard.
Vote: 3-0 · Source: https://docs.nango.dev/guides/auth/auth-guide (primary)
Quote: "Pre-built UI — Embedded Connect UI with API-specific guidance, input validation, and your branding."
Verifier evidence (high): The primary source (Nango auth-guide, https://nango.dev/docs/guides/auth/auth-guide) directly confirms all four steps verbatim: (1) "Set up a backend endpoint that your frontend will call before each authorization attempt to retrieve a session token from Nango." (2) "Load the Nango frontend SDK, fetch the session token from your backend, and open Connect UI." (3) user authorizes the external API in the Connect UI modal. (4) "When authorization succeeds, Nango generates a unique connection ID... The connection information arrives through a server-side webhook... payload includes 'connectionId': '<CONNECTION-ID>'." Two independent corroborations: WebSearch results and the getting-started authorize-an-api doc describe the identical sequence (backend session token -> frontend Connect UI -> authorize -> webhook with connection ID + tags). Source quality is high (current vendor primary docs using the modern connect-session flow, not the deprecated public-key flow). Caveats that do NOT rise to refutation: (a) the cited SUPPORTING QUOTE ("Pre-built UI — Embedded Connect UI...") only covers the UI branding, not the four-step sequence — but the source URL it points to fully substantiates the claim; (b) the claim frames the webhook as the way the backend learns the connection ID and omits that Nango also exposes a frontend onEvent/onComplete callback as an alternative — a minor incompleteness, not an error, and the docs themselves recommend the webhook for persistence ("You must store it on your side"). The claim is accurate, current, and well-sourced.

### [9] Nango models each authorized integration as a per-user 'Connection' object that stores one user's credentials for one external API and keeps them valid over time, with tags like end_user_id, end_user_email, and organization_id to reconcile connections to specific users and orgs — directly relevant to Fyralis's per-user vs org-wide tenant profile model.
Vote: 3-0 · Source: https://docs.nango.dev/guides/auth/auth-guide (primary)
Quote: "A Connection is created after each successful authorization. It stores one user's credentials for one external API and keeps them valid over time."
Verifier evidence (high): The claim is verbatim-supported by Nango's official current docs (nango.dev/docs/guides/auth/auth-guide, canonical redirect of the cited URL). The page states exactly: "A Connection is created after each successful authorization. It stores one user's credentials for one external API and keeps them valid over time." — matching the supporting quote word-for-word. The tags portion is also confirmed by two independent Nango docs pages: the auth-guide says tags "reconcile Nango's auth-success webhook with the right user, org, or entity in your system" and recommends starting with exactly end_user_id, end_user_email, organization_id; the connections API reference (nango.dev/docs/reference/api/connections/get, ConnectionFull schema) shows the connection object contains end_user (id, email, display_name), an organization object (id), tags (key/value strings), and credentials. No source disputes or qualifies the core claim. One minor nuance: the nested `organization` field under end_user is marked deprecated in the API schema, but organization_id as a TAG (which is what the claim references) is current — so this does not undermine the claim. Source quality is high (primary vendor docs, current as of 2026). The "directly relevant to Fyralis's per-user vs org-wide tenant model" is a reasonable, non-extraordinary relevance assertion given Fyralis's stated org-aware tenant profile.

### [10] Nango's @nangohq/frontend SDK is initialized in the browser with a connect session token (not the secret key), and calling nango.auth('<INTEGRATION-ID>') opens a modal/popup for the user to log in to the external account, returning a promise that resolves with providerConfigKey and connectionId on success or rejects on failure.
Vote: 3-0 · Source: https://docs.nango.dev/guides/api-authorization/authorize-in-your-app-custom-ui (primary)
Quote: "import Nango from '@nangohq/frontend'; const nango = new Nango({ connectSessionToken: '<CONNECTION-SESSION-TOKEN>' }); ... "For OAuth, this will open a modal to let the user log in to their external account." ... On success, the promise resolves with providerConfigKey (the integration ID) and connectionId. On failure, it rejects with an error object containing type and message fields (e.g., 'authorization_cancelled')."
Verifier evidence (high): Nango's canonical Frontend SDK reference (https://nango.dev/docs/reference/sdks/frontend, the live post-redirect host of docs.nango.dev) confirms every element of the claim. (1) Initialization: `new Nango({ connectSessionToken: '<CONNECTION-SESSION-TOKEN>' })`; publicKey is deprecated; the secret key is never used in the browser — so "connect session token, not the secret key" is accurate. (2) nango.auth('<INTEGRATION-ID>'): for OAuth flows "the method will open a modal to let the user log in to their external account" — confirmed verbatim. (3) Success: the promise returns an object containing `providerConfigKey` (the integration ID) and `connectionId`. (4) Failure: rejects with an error object having `type` (e.g., `authorization_cancelled`) and `message` fields (also exports an AuthError class). The WebSearch also independently corroborated: SDK init via connectSessionToken, auth() returns a promise (.then/.catch), connectionId is a Nango-generated UUID you persist, providerConfigKey = integration ID. CAVEAT: the exact cited URL (/guides/api-authorization/authorize-in-your-app-custom-ui) now 301-redirects and the redirected path 404s due to a docs restructure, but the substance is fully corroborated by the higher-authority live SDK reference and is current (publicKey deprecation reflects recent connect-session model). No contradicting evidence found.

### [11] The connect session token is created server-side via the Nango POST /connect/sessions endpoint authenticated with the backend Nango secret key, and the frontend fetches this short-lived token before each authorization attempt — establishing the security boundary so the secret key never reaches the browser.
Vote: 3-0 · Source: https://docs.nango.dev/guides/api-authorization/authorize-in-your-app-custom-ui (primary)
Quote: "The backend creates a connect session token via the Nango API endpoint POST /connect/sessions using the Nango secret key: "Set up a backend endpoint that your frontend will call before each authorization attempt to retrieve a session token from Nango" ... The session token acts as the security boundary—it's a short-lived credential generated server-side with the secret key, then transmitted to the browser where the frontend SDK uses it to authenticate the Connect UI flow. The secret key remains backend-only."
Verifier evidence (high): Claim is fully corroborated by primary Nango documentation across multiple pages. (1) The Create-a-connect-session API reference (nango.dev/docs/reference/api/connect/sessions/create) confirms POST /connect/sessions "Creates a short-lived connect session (30m)" and uses bearer auth with "The secret key from your Nango environment" (scope environment:connect_sessions:write) — confirming server-side creation with the secret key and short-lived nature. (2) The custom-UI guide (authorize-in-your-app-custom-ui) confirms the frontend flow: "retrieve the session token from your backend via a fetch request, then call connect.setSessionToken(res.sessionToken)," and Nango's own wording is to "Set up a backend endpoint that your frontend will call before each authorization attempt." (3) Nango's Security docs and public-key-deprecation page confirm the secret key is backend-only: "This secret key should never be exposed to the frontend," and "The Nango public key is replaced with a short-lived Connect session token, fetched from your backend and passed to your frontend... secure by default." This is the CURRENT recommended pattern (it replaced the deprecated public key / HMAC flow), so not outdated. No contradicting evidence found. Minor nuance: "before each authorization attempt" is Nango's own operational guidance, not a strict per-request mint (token is reusable within its 30m window) — but this matches Nango's documented phrasing exactly and is not an overreach. Sources are primary technical docs, not marketing.

### [12] Nango provides a pre-built, embeddable Connect UI for OAuth/auth flows that handles API-specific guidance, input validation, and host-app branding, presenting either an integration picker or sending the user straight into a single integration's auth flow.
Vote: 3-0 · Source: https://nango.dev/docs/guides/auth/auth-guide (primary)
Quote: "Embedded Connect UI with API-specific guidance, input validation, and your branding"
Verifier evidence (high): Every element of the claim is corroborated by Nango's primary documentation. (1) The exact supporting quote "Embedded Connect UI with API-specific guidance, input validation, and your branding" is confirmed on the cited primary source (nango.dev/docs/guides/auth/auth-guide). (2) Pre-built/embeddable + branding confirmed; the docs note "if you need full UX control, follow Customize Connect UI." (3) Input validation is a documented standalone feature in Nango's product changelog (nango.dev/product-updates/auth-input-validation): "available only for integrations using Nango's Connect UI." (4) The picker-vs-direct-flow behavior is confirmed via the allowed_integrations session parameter on a second Nango docs page (nango.dev/docs/guides/primitives/auth): multiple integrations -> "Connect UI shows a picker"; a single integration -> "Connect UI sends the user straight to its auth flow." The claim accurately paraphrases the source rather than overreaching. Source quality (vendor's own current technical docs) is appropriate for a factual product-capability claim, and the load-bearing picker detail is independently corroborated across two Nango pages. No contradicting evidence found; claim is current (docs reference active products/pricing).

### [13] The embedded connector-auth flow is secured via a short-lived session token that the host app's backend must mint server-side before each authorization attempt; the frontend SDK exchanges that token to open the Connect UI.
Vote: 3-0 · Source: https://nango.dev/docs/guides/auth/auth-guide (primary)
Quote: "Set up a backend endpoint that your frontend will call before each authorization attempt to retrieve a session token."
Verifier evidence (high): The claim is well-supported by the primary source and corroborated by Nango's official docs. (1) Quote support: Nango's auth-guide states verbatim "Set up a backend endpoint that your frontend will call before each authorization attempt to retrieve a session token from Nango" — directly backing "minted server-side before each authorization attempt." (2) Architecture corroboration (docs.nango.dev/guides/getting-started + reference/sdks/frontend): "Backend — your server asks Nango for a short-lived session token. Frontend — your app opens the Nango Connect UI with that token." The backend mints it via the server-side `nango.createConnectSession()` (secret-key call), and the frontend SDK (@nangohq/frontend) initializes with `connectSessionToken` / calls `connect.setSessionToken(res.sessionToken)` to open Connect UI — exactly the "frontend SDK exchanges that token" described. (3) Short-lived: docs explicitly call the session token "short-lived" (related direct connect_link is "valid 30 minutes"). Source quality is sufficient: this is the vendor's own current primary documentation describing a live SDK and endpoints, appropriate for a claim about that vendor's own mechanism. No contradicting or qualifying source surfaced in search — all results (GitHub repo, multiple doc pages, third-party blog) describe the identical flow. Not a marketing/benchmark claim; it is a technical mechanism description. No overreach: every component of the claim maps to documented behavior.

### [14] Nango stores external-source credentials in encrypted storage with automatic refresh and retrieves/uses them at scale (e.g. via Proxy or Functions) so that raw credentials never pass through the host application's codebase.
Vote: 3-0 · Source: https://nango.dev/docs/guides/auth/auth-guide (primary)
Quote: "Credentials can be retrieved at scale or used directly by other Nango primitives — without ever passing through your codebase."
Verifier evidence (high): CLAIM SUPPORTED. The primary source (Nango auth-guide, https://nango.dev/docs/guides/auth/auth-guide) directly states the credential-handling mechanism and lists "Encrypted storage, automatic refresh, retrieval at scale," with the verbatim quote "credentials can be retrieved at scale or used directly by other Nango primitives — without ever passing through your codebase" and "Combine with Proxy or Functions to avoid handling credentials directly." The claim accurately paraphrases this — no overreach. Independent corroboration: Nango's Security page (https://nango.dev/docs/guides/platform/security) confirms credentials (OAuth tokens, API keys, client secrets) are encrypted at rest with AES-256-GCM (256-bit key, 12-byte IV, 16-byte auth tag) plus TLS 1.2+ in transit; the Proxy guide confirms the Proxy resolves the provider and injects credentials into outbound requests, keeping them out of app code. Source quality is appropriate: this is a product-mechanism claim, so the vendor's own official docs are the authoritative primary source, and they are current (live June 2026). The only qualifier surfaced (HN/Truto-cited shared-Lambda warm-environment isolation risk for Functions) concerns customer-to-customer isolation when running untrusted code, NOT whether raw credentials traverse the HOST application's codebase — so it does not refute this specific claim. Two of three skeptical refutation angles (unsupported-by-quote, contradicted, low-quality-source) fail to land.

### [15] Public OAuth clients (e.g. SPA/native connector-auth widgets) MUST use PKCE, and authorization servers MUST support PKCE, for the authorization code flow.
Vote: 3-0 · Source: https://datatracker.ietf.org/doc/rfc9700/ (primary)
Quote: "Public clients MUST use PKCE [RFC7636] to this end, as motivated in Section 4.5.3.1. ... Authorization servers MUST support PKCE [RFC7636]."
Verifier evidence (high): Claim is fully supported by the primary source RFC 9700 (IETF Best Current Practice, published Jan 2025), Section 2.1.1 (Authorization Code Grant). Verbatim normative text confirmed via two independent fetches (datatracker.ietf.org and rfc-editor.org/rfc/rfc9700.html): "Public clients MUST use PKCE [RFC7636]" and "Authorization servers MUST support PKCE [RFC7636]." Both use the RFC 2119 MUST keyword (normative requirement, not SHOULD/RECOMMENDED). The claim is correctly scoped: the absolute MUST applies to public clients (the claim cites SPA/native connector-auth widgets — exactly the public-client case), whereas RFC 9700 only RECOMMENDS PKCE for confidential clients, so the claim does not overreach. The claim also correctly limits PKCE to the authorization code flow. Source quality (primary IETF RFC, current, BCP) matches the claim strength. Corroborated by multiple secondary sources (WorkOS, Scalekit, Strivacity, RFC Editor) and reinforced by OAuth 2.1. No contradicting or qualifying evidence found across the five checklist axes.

### [16] Authorization servers MUST use exact string matching of redirect URIs against pre-registered URIs (the only exception being port numbers for localhost native-app redirects), rather than prefix or wildcard matching.
Vote: 3-0 · Source: https://datatracker.ietf.org/doc/rfc9700/ (primary)
Quote: "When comparing client redirection URIs against pre-registered URIs, authorization servers MUST utilize exact string matching except for port numbers in localhost redirection URIs of native apps."
Verifier evidence (high): The claim is fully supported by primary-source verbatim text. RFC 9700 (Best Current Practice for OAuth 2.0 Security, IETF, Jan 2025), confirmed via BOTH datatracker.ietf.org/doc/rfc9700/ and the official rfc-editor.org/rfc/rfc9700.html, states verbatim: "When comparing client redirection URIs against pre-registered URIs, authorization servers MUST utilize exact string matching except for port numbers in `localhost` redirection URIs of native apps." It further states: "The only exception is native apps using a `localhost` URI: In this case, the authorization server MUST allow variable port numbers" (per RFC 8252 Sec 7.3). The RFC explicitly rejects prefix/wildcard/pattern matching, calling it "more complex to implement and more error-prone to manage" and the cause of real-world breaches (wildcard flaws, subdomain takeovers). The claim's paraphrase ("MUST use exact string matching... only exception localhost native-app port numbers... rather than prefix or wildcard matching") is faithful, not an overreach. No contradicting credible source found; WorkOS, Strivacity, and Connect2id corroborate. The apparent "contradiction" in search results is the RFC merely noting that real-world providers DEVIATE from the requirement — which does not dispute the mandate itself. Source quality (authoritative IETF standard) exceeds the claim's strength, and it is the current/newest OAuth security guidance (not outdated). Minor nuance: localhost-port is the sole exception to the MATCHING ALGORITHM specifically; the RFC has separate provisions on http/loopback scheme and open redirectors, but these don't undercut the claim as scoped.

### [17] The implicit grant SHOULD NOT be used and the resource owner password credentials grant MUST NOT be used; the secure pattern for connector OAuth is the authorization code flow.
Vote: 3-0 · Source: https://datatracker.ietf.org/doc/rfc9700/ (primary)
Quote: "Clients SHOULD NOT use the implicit grant (response type token) or other response types issuing access tokens in the authorization response. ... The resource owner password credentials grant [RFC6749] MUST NOT be used."
Verifier evidence (high): Claim is verbatim-faithful to the primary source RFC 9700 (Best Current Practice for OAuth 2.0 Security, IETF, published Jan 2025). WebFetch of datatracker.ietf.org/doc/rfc9700/ confirms the exact normative language: implicit grant "SHOULD NOT use the implicit grant (response type token)" and ROPC "MUST NOT be used," with the authorization code grant (response type code, with PKCE) named as the recommended alternative. The claim preserves the precise normative force (SHOULD NOT for implicit, MUST NOT for ROPC) without overreach. Multiple independent credible sources corroborate (oauth.net, WorkOS blog "we read RFC 9700," Scalekit, Strivacity docs). No contradicting or qualifying source found. Source is the highest-authority primary standard for this exact claim; current (latest OAuth Security BCP, ~17 months old); not marketing. Refutation failed on all five checklist axes.

### [18] Merge Link is a drop-in, embeddable UI component (with official SDKs for React, Vue, and vanilla JS) that end users open to pick an integration, sign in with their provider, and authorize data access — directly validating the 'embeddable connector-auth widget' pattern for source onboarding.
Vote: 3-0 · Source: https://docs.merge.dev/get-started/link/ (primary)
Quote: "a drop-in UI component for React, Vue, or vanilla JS that your end user opens to pick an integration, sign in with their provider, and authorize data access"
Verifier evidence (high): Every element of the claim is independently verified against Merge's own primary artifacts. (1) Drop-in/embeddable UI component: corroborated wording across Merge docs mirrors — "Merge Link is a UI component that guides your app's user through setting up an integration" via "a series of token exchanges to securely authenticate your users' integrations." (2) Official React SDK: @mergeapi/react-merge-link on npm (MIT, v2.2.4) and GitHub repo merge-api/react-merge-link, updated 2026-06-15 (verified via gh api) — official, actively maintained. (3) Official Vue SDK: @mergeapi/vue-merge-link on npm + GitHub repo merge-api/vue-merge-link under the official merge-api org (desc "Vue wrapper for Merge link"), verified via gh api. (4) Vanilla JS: confirmed via the script-tag loader https://cdn.merge.dev/initialize.js, which "works without requiring React, Vue, or any other framework." (5) End-user flow (pick integration, sign in with provider, authorize): confirmed — Link "guides your app's user through setting up an integration" and authenticates the user's integration via token exchange. No contradicting source found. Claim is current (React SDK updated one week ago). Not marketing fluff — it is verifiable in shipping packages. Minor caveat: the exact primary URL docs.merge.dev/get-started/link/ rendered as a client-side SPA and returned 'Page Not Found' to the automated fetcher, so the wording was confirmed via search snippets + the canonical npm/GitHub/CDN artifacts rather than a direct fetch — but substance is fully corroborated.

### [19] Connector auth uses a three-token exchange: a temporary backend-created link_token initializes the Link session, a short-lived public_token is returned to the frontend onSuccess callback when the user finishes, and the backend exchanges that for a permanent account_token — keeping the long-lived credential off the client.
Vote: 3-0 · Source: https://docs.merge.dev/get-started/link/ (primary)
Quote: "link_token: Temporary token to initialize your end user's Merge Link."
Verifier evidence (high): All three legs of the claim are confirmed by Merge's own primary docs/help center. (1) link_token: Merge docs (surfaced via search of merge.dev) — "Request a single-use link_token from Merge to initialize an integration authorization session for your user"; help.merge.dev article 5389539 calls it "the first of three separate tokens... the temporary authentication token between your customer completing the linking flow and Merge." (2) public_token: "When your user completes integration authorization, the component will return a public_token via the onSuccess handler" — matching the claim's "short-lived public_token returned to the frontend onSuccess callback." (3) account_token: "Pass this short-lived public_token to your backend to securely exchange it with Merge for a permanent account_token"; help article: account_token is "the final, permanent token... It's up to your back-end to properly store this." The "long-lived credential off the client" framing is accurate — only the short-lived public_token reaches the frontend; the permanent account_token is backend-only. The claim's cited supporting quote ("link_token: Temporary token to initialize...") is only a partial citation, but the FULL three-token claim is independently and explicitly supported. This is Merge's current, documented model (a direct parallel to Plaid's link_token/public_token/access_token). Note: the exact cited URL https://docs.merge.dev/get-started/link/ now returns 404 (doc reorg), but the identical content is live at help.merge.dev/en/articles/5389539 and on the merge.dev/docs get-started/link page.

### [20] The permanent account_token (the credential used for all future API requests on behalf of the end user's connected source) must be stored server-side in the developer's database, never held client-side — a concrete secret-handling / least-privilege-of-exposure pattern for a multi-tenant secret store.
Vote: 3-0 · Source: https://docs.merge.dev/get-started/link/ (primary)
Quote: "Securely store this `account_token` in your database for authenticating future API requests to the Unified API regarding the end user's data."
Verifier evidence (high): The cited source (docs.merge.dev/get-started/link/) contains, per search-indexed content, the near-verbatim instruction: "Securely store the account_token in your database for authenticating future API requests to the Unified API regarding the end user's data." Merge's Authentication doc reinforces this: "you'll only be authorized to access or manipulate that users' data if they've gone through Merge Link and you've successfully stored their account_token," used as the X-Account-Token header on server-side API requests. So the core of the claim — permanent account_token stored server-side in the developer's DB, used for all future API requests on the end user's behalf — is directly supported by the primary source. The "never held client-side" framing is an interpretive gloss not literally in the Merge quote, but it is a correct and well-corroborated one: OWASP's OAuth2 Cheat Sheet, Auth0, Curity, and FusionAuth all independently establish that long-lived API credentials belong server-side, never in browser localStorage (XSS exposure). Source quality (primary vendor docs + multiple independent authorities) matches the claim's strength; the practice is current, stable, and not marketing. (Note: WebFetch returned 404s on the doc URLs, but this is a JS-rendered-SPA fetch artifact — search indexing confirms the exact text lives on the cited page.) Minor caveat: the literal source says "store in your database," and the claim's "never client-side" is an added inference, but it is standard and accurate, not a distortion.

### [21] B2B SaaS activation averages 37.5% across 62 companies, and two-thirds of new SaaS users never activate, making activation (not signup) the central onboarding metric.
Vote: 2-1 · Source: https://www.digitalapplied.com/blog/customer-onboarding-time-to-value-2026-saas-metrics-framework (secondary)
Quote: ""Activation averages 37.5%" across 62 B2B SaaS companies (Userpilot 2024) ... "Two-thirds of new SaaS users never activate""
Verifier evidence (high): The core statistic is verified against the named PRIMARY source. The Userpilot "User Activation Rate Benchmark Report 2024" (https://userpilot.com/blog/user-activation-rate-benchmark-report-2024/) states verbatim: "The average activation rate is 37,5%, and the median activation rate is 37%," "based on data from 62 B2B companies collected with Userpilot's New User Activation dashboard." So "activation averages 37.5% across 62 companies (Userpilot 2024)" is accurately attributed and exactly matches the primary source. The "two-thirds of new SaaS users never activate" portion does NOT appear in the Userpilot primary report (WebFetch confirmed: "The report does not include any statement claiming that two-thirds of new users never activate") — it is the secondary digitalapplied.com blog's arithmetic restatement (100% - 37.5% = 62.5%, rounded to "roughly two-thirds"). That is a fair derivation of the same datum, not a fabrication or meaning-changing overreach. The "central metric" framing is editorial but defensible. Main genuine weakness (qualifies, not refutes): activation lacks a standard definition — "almost endless flexibility in the definition of activation" and OpenView self-reported medians ran ~50%, making cross-company comparison fragile (prodpad.com, lennysnewsletter.com). Data is 2024, current enough for a 2026 context. Verdict: load-bearing claim traces cleanly to a primary source with matching numbers; the derived "two-thirds" is arithmetically sound. Not refuted.

### [22] Onboarding completion is driven by progress visibility: a partially-complete checklist/progress bar motivates users more than a blank state, and effective onboarding checklists contain three to five items tied to activation milestones with doc links and exit/skip options.
Vote: 3-0 · Source: https://www.appcues.com/blog/saas-user-onboarding (blog)
Quote: "People are motivated to complete things they've started. A progress bar at 40% completion is more motivating than a blank slate."
Verifier evidence (high): The claim has two parts, both well-supported. (1) "Partial progress motivates more than a blank state" is the endowed-progress/Zeigarnik/goal-gradient effect — a documented psychological principle, not an Appcues invention. The source quote ("A progress bar at 40% completion is more motivating than a blank slate") directly supports it, and it is independently corroborated (Userpilot "Psychology Behind Progress Bars"; goal-gradient literature; the well-established Kivetz et al. endowed-progress research that "1 of 5 complete" beats "0 of 5"). (2) WebFetch of the source confirmed it states VERBATIM: "A well-designed checklist includes three to five items tied directly to activation milestones, links to supporting documentation, access to help resources, and a clear exit option" — so the claim is an accurate paraphrase, not an overreach. The "3-5 items" figure is a widely-repeated industry heuristic (Userpilot, Userflow's four-task design), so no extraordinary primary source is needed. Attempted refutations failed: the contradicting evidence I found (clientsuccess.com, aakashg.com: "completion theater," "tour completion is a vanity metric," checklist completion ≠ retention) critiques the CAUSAL link between completion and real activation/retention — a DIFFERENT claim. It does not dispute that partial progress motivates completion; it actually presupposes checklists drive completion behavior. The thisisglance.com "manipulation" critique likewise concedes progress bars DO motivate (calling it manipulative), reinforcing rather than refuting the mechanism. Source is a vendor blog (Appcues sells onboarding software = commercial interest), but the claim is moderate-strength and the mechanism is academically grounded, so source quality matches claim strength. Not outdated (repeated through 2026 sources).

### [23] Onboarding should be goal-oriented around a single specific activation action rather than exposing every feature, with the flow ending in the user doing the core action (not watching a demo).
Vote: 3-0 · Source: https://www.appcues.com/blog/saas-user-onboarding (blog)
Quote: "A tour that ends with the user having watched a demo is less effective than one that ends with the user having done the thing."
Verifier evidence (high): The claim is well-supported and corroborated, so it survives skeptical review. (1) Quote support: The supporting quote is verbatim-accurate on the appcues.com page, and the article's surrounding text directly backs BOTH halves of the claim. For "goal-oriented around a single activation action rather than exposing every feature": "A good onboarding experience is not a laundry list of everything you can do with your product. It is a flow that ends with a user taking a desired action..." and the Slack example "does not try to teach every feature. Instead, it focuses on getting users to send their first message in a channel, because that single action is the gateway to daily use." For "doing the core action, not watching a demo": the cited quote itself. No overreach. (2) Independent corroboration (not just the vendor blog): Nielsen Norman Group (nngroup.com/articles/onboarding-tutorials) — push-style tutorials are "disruptive, often skipped, and poorly recalled," contextual on-demand help performs better; the activation-metric literature names the same single-action pattern (Slack first message, Dropbox first file upload); Jimo's analysis of 1,025 product tours found median completion ~15% with every step past 3-4 killing completion; Stanford Learning Lab cited that active engagement yields ~70% better retention than passive consumption. (3) Source quality: Appcues is an onboarding-tooling vendor with commercial interest (a weakness), but this is a mainstream, non-extraordinary best practice, and it is independently confirmed by a primary UX-research authority (NN/G) and academic/aggregated-data sources, so quality matches claim strength. (4) Not outdated — reaffirmed in 2025/2026 sources. (5) Not solely marketing fluff given the non-vendor corroboration. Minor caveat: enterprise/B2B sources note this works best layered with CSM/human-assist touchpoints for complex products, but that qualifies HOW to apply it, not WHETHER the principle holds — and Fyralis's stated onboarding model already includes optional human assist.


## Refuted claims (for transparency)
- "Over 98% of new users churn within two weeks if they do not reach a value milestone, establishing a hard time-to-value deadline for onboarding design." (https://www.digitalapplied.com/blog/customer-onboarding-time-to-value-2026-saas-metrics-framework, vote 1-2)

