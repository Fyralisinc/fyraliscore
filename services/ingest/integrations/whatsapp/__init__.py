"""WhatsApp (WhatsApp Business Platform / Cloud API) integration package.

Phase 1 ships LIVE ingestion only (Meta-signed webhooks). Future phases add a
Graph API client (media download), OAuth/Embedded-Signup onboarding, and the
deferred backfill avenues (Coexistence history webhook / BSP bulk export).
"""
