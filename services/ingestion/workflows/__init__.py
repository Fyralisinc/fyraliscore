"""Temporal workflow definitions for ingestion (M2+). Per LLD §9.

Will contain: OnboardingTriggerPollerWorkflow (poller.py),
TenantOnboardingWorkflow (tenant.py), SourceOnboardingWorkflow
(source.py), ShardFetchWorkflow (shard.py), *Args dataclasses (args.py).
"""
