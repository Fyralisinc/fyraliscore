# Grafana alerting notification policy tree (provisioned as-code).
apiVersion: 1

policies:
  - orgId: 1
    receiver: fyralis-ops
    group_by: ['alertname']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
